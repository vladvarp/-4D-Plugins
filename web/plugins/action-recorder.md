---
title: Action Recorder
tagline: Записывай и воспроизводи операции Cinema 4D как Python-скрипты — фиксируй каждое действие сцены и повторяй его в один клик.
version: v1.14
author: V.Ar Production
updated: Июнь 2026
icon: ico/ActionRecorder/Manager.png
tags: [Автоматизация]
license: GPL v3
cinema4d: R26+
os: Windows
renderer: Все рендереры
download: https://github.com/vladvarp/-4D-Plugins/releases/download/C4D/4DPluginInstaller.exe
github: https://github.com/vladvarp/-4D-Plugins
---

## О плагине

===photo -s600*400 -c2 -asc1000 -asp1000
[[p:'image/ActionRecorder/1.png']]
[[p:'image/ActionRecorder/2.png']]
[[p:'image/ActionRecorder/3.png']]
===

[[mdb:'UI Action Recorder'-n'AR'-b8]] [[mdb:'UI Log'-n'ARL'-b8]]

Action Recorder — инструмент для записи и воспроизведения операций Cinema 4D в виде Python-скриптов. Плагин перехватывает все изменения сцены (добавление/удаление объектов, трансформации, изменения параметров) и преобразует их в исполняемый код.

Записанные операции сохраняются как `.py` файлы в папке `~/Documents/ActionRecorder/` и могут быть запущены повторно в любом проекте. Это позволяет автоматизировать рутинные задачи: создание стандартных объектов, позиционирование, настройку рендера.

:::info
Плагин находится во вкладке AR в главном меню. Состоит из трёх команд: Лог событий, Менеджер операций и Воспроизвести последний.
:::

## Возможности

::::features
### 🔴 Запись изменений сцены
Автоматический перехват всех действий: добавление/удаление объектов, трансформации, модификация параметров. Запись ведётся через глобальный хук `EVMSG_CHANGE`.

### 📋 Лог событий
Просмотр всех зафиксированных изменений в виде форматированного Python-кода с временны́ми метками. Лог обновляется в реальном времени.

### 📂 Коллекции и операции
Организация операций в именованные коллекции (папки). Каждая операция — отдельный `.py` файл с кодом.

### ▶️ Воспроизведение
Запуск любой операции одной кнопкой. Код выполняется в контексте активного документа с доступом к `c4d` и `doc`.

### ✎ Редактирование кода
Встроенный редактор Python-кода с подсветкой синтаксиса. Можно вручную отредактировать записанный код перед запуском.

### ⚡ Быстрое воспроизведение
Кнопка «Воспроизвести последний» запускает последнюю использованную операцию без открытия менеджера.
::::

## Установка

::::steps
### Скачай программу
[[button:4DPluginInstaller.exe|https://github.com/vladvarp/-4D-Plugins/releases/download/C4D/4DPluginInstaller.exe]]
### Выбрать папку плагинов Cinema 4D
### Установи пакет [[badge:Beta|Action Recorder]]
### Плагин появится во вкладке [[badge:Beta|AR]]
::::

:::info
Папка плагинов Cinema 4D на Windows: `C:\Users\<имя>\AppData\Roaming\Maxon\Cinema 4D R26\plugins`
:::

## Использование

### Запись операции

1. Открой Менеджер операций: вкладка `AR` → `Менеджер операций`
2. Создай новую коллекцию или выбери существующую
3. Нажми **+ Новый** для создания операции
4. Нажми **●** рядом с операцией для начала записи
5. Выполняй любые действия в сцене — все изменения записываются автоматически
6. Нажми **■** для остановки записи

:::tip
Запись работает через глобальный хук `EVMSG_CHANGE`. Добавление примитивов через палитру автоматически распознаётся как `c4d.CallCommand()`.
:::

### Воспроизведение

1. Выбери операцию в списке
2. Нажми **▶** для запуска
3. Код выполнится в контексте текущего документа

Или используй кнопку **AR: Воспроизвести последний** для быстрого запуска последней операции.

### Редактирование кода

1. Выбери операцию в списке
2. Отредактируй код в правой панели
3. Нажми **Сохранить код** или используй горячую клавишу
4. Статус сохранения отображается в нижней части редактора

:::warning
При ручном редактировании кода убедись, что синтаксис Python корректен. Ошибки в коде приведут к сбою при воспроизведении.
:::

## Менеджер операций

::::details-open
Элементы управления

| Элемент | Описание |
|---------|----------|
| **Коллекция** | Выпадающий список коллекций (папок с операциями) |
| **+ Новая** | Создать новую коллекцию |
| **Удалить** | Удалить текущую коллекцию и все её операции |
| **+ Новый** | Создать новую операцию в текущей коллекции |
| **▶** | Воспроизвести выбранную операцию |
| **●/■** | Начать/остановить запись |
| **✎** | Переименовать операцию |
| **✕** | Удалить операцию |
| **Код операции** | Редактор Python-кода с подсветкой синтаксиса |
| **Сохранить код** | Сохранить изменения из редактора в файл |
| **Восстановить** | Откатить изменения из редактора к сохранённой версии |
::::

## Лог событий

::::details-open
Элементы управления

| Элемент | Описание |
|---------|----------|
| **Статус** | Индикация активности лога |
| **Очистить** | Очистить текущий лог (не удаляет файлы операций) |
| **Сохранить как операцию** | Сохранить содержимое лога как новую операцию в коллекции |
::::

:::info
Лог обновляется автоматически при каждом изменении сцены. Каждое событие помечено временны́й меткой в формате `HH:MM:SS`.
:::

## Структура хранения

::::details
Файловая система

```
~/Documents/ActionRecorder/
├── Коллекция/          ← папка коллекции
│   ├── Операция1.py    ← файл операции
│   └── Операция2.py
├── Другая коллекция/
│   └── ...
└── _last_action.json   ← мета-информация о последней операции
```

Каждый `.py` файл операции содержит:

```python-num
# Action: Имя операции
# Recorded: 2026-06-29 14:30:00
# --- ваш код ---
```
::::

## Ограничения

- Запись работает только при открытом диалоге Лога или Менеджера
- Сложные операции (программное создание объектов через Python) могут не распознаваться корректно
- Операции не зависят от сцены — при воспроизведении объекты создаются в текущем документе
- Имена объектов в коде привязаны к именам при записи — переименование объектов в сцене может слить поиск

:::warning
При воспроизведении операции создают новые объекты параллельно существующим. Если в сцене уже есть объект с таким именем — `SearchObject` вернёт первый найденный, что может привести к неожиданным результатам.
:::

## Состав

| Иконка | Иконка | Название | Назначение |
|--------|--------|----------|------------|
| [[ico:'ico/ActionRecorder/Manager.png']] | Менеджер операций | Вкладка AR | Открытие менеджера |
| [[ico:'ico/ActionRecorder/Log.png']]     | Лог событий       | Вкладка AR | Открытие окна лога |
| [[ico:'ico/ActionRecorder/Play.png']]    | Воспроизвести     | Вкладка AR | Быстрый запуск последней операции |




<mdc-n'AR'-s950*100>
icon: ico/ActionRecorder/Manager.png
title: Action Recorder — Менеджер операций
tagline: Action Recorder — создание, удаление, запуск операций.

<column-l900>
  <col-l70>
  Коллекция:
  </col>
  <col-l600-c1>
  [[ddl:'Коллекция','Проект #1','Проект #2','Проект #3','Тестовые'-s-p3]]
  </col>
  <col-l200-c3>
  [[mdb:'+ Новая'-n'0']] [[mdb:'✕ Удалить'-n'0']]
  </col>
</column>
---
<column-l900>
  <col-l380>
  Операции:
  [[mdb:'Рендер 1000*1000'-n'0'-s170]]  [[mdb:'▶'-n'0']] [[mdb:'●'-n'0']] [[mdb:'✎'-n'0']] [[mdb:'✕'-n'0']]
  [[mdb:'> Начальная сцена'-n'0'-s170]] [[mdb:'▶'-n'0']] [[mdb:'■'-n'0']] [[mdb:'✎'-n'0']] [[mdb:'✕'-n'0']]
  [[mdb:'Чистка'-n'0'-s170]]            [[mdb:'▶'-n'0']] [[mdb:'●'-n'0']] [[mdb:'✎'-n'0']] [[mdb:'✕'-n'0']]
  [[mdb:'Сортировка'-n'0'-s170]]        [[mdb:'▶'-n'0']] [[mdb:'●'-n'0']] [[mdb:'✎'-n'0']] [[mdb:'✕'-n'0']]
  [[mdb:'Деформер'-n'0'-s170]]          [[mdb:'▶'-n'0']] [[mdb:'●'-n'0']] [[mdb:'✎'-n'0']] [[mdb:'✕'-n'0']]
  [["1"]]
  [[mdb:'+ Новый'-n'0'-s]]
  ---
  [[mdb:'Лог операций'-n'0'-s]]
  [[mdb:'Протокол скрипта'-n'0'-s]]
  </col>
  <col>
  Код операции:
  ```-num-l380
  # Action: Начальная сцена
  # Created: 2026-06-29 16:00:21
  # --- ваш код ---
  obj = c4d.BaseObject(5103)  # Камера
  obj.SetName('Камера')
  obj.SetAbsPos(c4d.Vector(600.0000, 300.0000, -600.0000))
  obj.SetAbsRot(c4d.Vector(0.7854, -0.3395, 0.0000))
  doc.InsertObject(obj)
  c4d.EventAdd()
  obj = doc.SearchObject('Камера')
  if obj:
      obj.SetAbsPos(c4d.Vector(0.0000, 0.0000, 0.0000))
      obj.SetAbsRot(c4d.Vector(0.0000, 0.0000, 0.0000))
      c4d.EventAdd()
  obj = doc.SearchObject('Камера')
  if obj:
      obj.SetAbsPos(c4d.Vector(0.0000, 0.0000, -362.3548))
      c4d.EventAdd()
  obj = doc.SearchObject('Камера')
  if obj:
      obj[1010] = 283.66798306452984  # param id=1010
      c4d.EventAdd()
  obj = doc.SearchObject('Камера')
  if obj:
      obj.SetAbsPos(c4d.Vector(0.0000, 147.2984, -362.3548))
      c4d.EventAdd()
  c4d.CallCommand(200000090)  # Вращать
  obj = doc.SearchObject('Камера')
  if obj:
      obj.SetAbsRot(c4d.Vector(0.0000, -0.3256, 0.0000))
      c4d.EventAdd()
  c4d.CallCommand(5186)  # Прямоугольник
  obj = doc.SearchObject('Прямоугольник')
  if obj:
      obj[2300] = 1  # param id=2300
      c4d.EventAdd()
  obj = doc.SearchObject('Прямоугольник')
  if obj:
      obj.SetAbsPos(c4d.Vector(0.0000, 200.0000, 0.0000))
      c4d.EventAdd()
  c4d.CallCommand(12236) # Конвертировать примитив
  obj = doc.SearchObject('Прямоугольник')
  if obj:
      obj[1002] = 0  # param id=1002
      c4d.EventAdd()
  obj = doc.SearchObject('Прямоугольник')
  if obj:
      obj.SetAbsRot(c4d.Vector(0.0000, 1.5708, 0.0000))
      c4d.EventAdd()
  ```
  ✓ Сохранено [["25"]] [[mdb:'↩ Восстановить'-n'0']] [[mdb:'Сохранить код ➜]'-n'0']]
  </col>
</column>
---
`● Запись в "Начальная сцена"...`
</mdc>

<mdc-n'ARL'-s650*100>
icon: ico/ActionRecorder/Log.png
title: Action Recorder — Лог событий
tagline: Action Recorder — просмотр лога команд в виде кода.
<column>
<col-l450>
`● Лог активен — записываются все изменения сцены`
</col>
<col-c3-l130>
[[mdb:'Очистить'-n'0']]
</col>
</column>
---
```-num
# [06:44:29]
rd = doc.GetActiveRenderData()
rd[c4d.RDATA_XRES] = 1280
rd[c4d.RDATA_YRES] = 1200
c4d.EventAdd()

# [06:44:33]
obj = c4d.BaseObject(5103)  # Камера
obj.SetName('Камера')
obj.SetAbsPos(c4d.Vector(600.0000, 300.0000, -600.0000))
obj.SetAbsRot(c4d.Vector(0.7854, -0.3395, 0.0000))
doc.InsertObject(obj)
c4d.EventAdd()
```
---
[[mdb:'Сохранить как операцию...'-n'0']]
</mdc>
